/**
 * @deprecated
 * The public `retry`, `retryWithSignal`, and `withRetry` functions have been
 * removed in v2.0.0. The retry engine is now internal and accessed through
 * `executeWithRetry` / `executeWithRetryAndSignal` in `engine.ts`.
 *
 * This file is intentionally empty. It will be deleted in Phase 3.
 */
